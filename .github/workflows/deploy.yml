name : Business Owner CI
on:
  push:
    branches : 
      - "main"
    
jobs:
  build:
    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v1
      - run : npm install
      - run : npm run build
      - run : sudo rm -r /var/www/html/business-owner/build
      - run : sudo mv /home/aakash/actions-runner/_work/business-owner/business-owner/build /var/www/html/business-owner/
      - run : sudo cp /var/www/.htaccess /var/www/html/business-owner/build

